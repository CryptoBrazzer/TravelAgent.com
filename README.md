# ESCAPE — AI Travel Agent website v7 MOTION + IMAGE INTEGRATION

This build fixes the latest two visual issues.

- The horizontal progress line at the top is removed.
- The full-page red dotted road is dynamic again: the dashes move continuously and route markers travel along it.
- The separate hero-only route is removed so there is one coherent journey line through the whole site.
- Images are no longer aggressively cropped. The complete artwork is shown with `object-fit: contain`.
- Image edges, shadows and surrounding glows are softened so the visuals blend into the page instead of looking pasted on.
- Desktop keeps text and visuals balanced; mobile keeps full images and a subtle moving road.
- Cache-busting is `?v=7`.

All seven JPG images remain in the repository root.


## v8 brand update
The visible Escape brand wordmark now uses a distinctive handwritten editorial/script treatment in red, inspired by luxury magazine masthead energy without reproducing a proprietary logo.
